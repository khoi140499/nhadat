<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_height="match_parent"
    tools:context=".Admin">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:paddingLeft="20dp"
        android:background="@color/colorSigup"
        android:gravity="center_vertical"
        >
        <TextView
            android:layout_width="80dp"
            android:layout_height="30dp"
            android:text="Xin chào:"
            android:gravity="center"
            android:textColor="@color/white"
            android:id="@+id/t12"
            />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:gravity="center"
            android:textColor="@color/red"
            android:textStyle="bold"
            android:id="@+id/admin_text"
            android:layout_toRightOf="@id/t12"
            android:layout_marginLeft="5dp"
            />

        <ImageButton
            android:id="@+id/btnlogout"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:layout_alignParentRight="true"
            android:layout_marginRight="10dp"
            android:background="@drawable/circleimg"
            android:src="@drawable/logout_rounded_left_25px" />
    </RelativeLayout>

    <ToggleButton
        android:layout_width="150dp"
        android:layout_height="40dp"
        android:layout_marginTop="5dp"
        android:layout_gravity="center"
        android:layout_marginBottom="5dp"
        android:id="@+id/admin_onoff"
        android:textOn="Duyệt"
        android:textOff="Chưa duyệt"
        android:background="@drawable/border"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/recycle_admin"

        />
</LinearLayout>